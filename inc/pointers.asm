#importonce

// usado para indicar que ocurrio una interrupcion de pantalla
.const loop_flag = $02

// contador global generico, util para disparar eventos cada n veces
.const global_counter = $03 

// status_flag
.const status_flag = $04


.const dummy_zero_page = $ff 